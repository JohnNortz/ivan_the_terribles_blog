<div class="comment">
  <h3>Comment <%= comment.id %>:</h3>
  <p><%= comment.body %></p>
  <div class="reply">
    <% comment.replies.each do |rep| %>
      <%= rep.body %>
    <% end %>
  </div>
</div>
